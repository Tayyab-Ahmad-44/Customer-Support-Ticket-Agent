import React from 'react';
import ReactDOM from 'react-dom/client';
import SupportTicketApp from './App.jsx'; // Ensure the correct path
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SupportTicketApp />
  </React.StrictMode>,
);